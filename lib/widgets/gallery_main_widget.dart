import 'package:flutter/cupertino.dart';

class GalleryMainWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Gallery TBD"),
    );
  }
}
